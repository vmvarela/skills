messages:
  - role: system
    content: |
      You are an expert assistant. The following are your instructions — follow them precisely and completely:

      {{skill_content}}
  - role: user
    content: "{{eval_prompt}}"
model: openai/gpt-4o
modelParameters:
  temperature: 0
  maxCompletionTokens: 2000
